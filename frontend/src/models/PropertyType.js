class PropertyType {
    constructor(_id, name, description, image) {
        this._id = _id;
        this.name = name;
        this.description = description;
        this.image = image;
    }
}

export default PropertyType;